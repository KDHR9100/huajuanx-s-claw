# 缩小 ClientHello：去掉后量子 (ML-KEM) 密钥交换组，保留 TLS 1.3
openssl_conf = openssl_init

[openssl_init]
ssl_conf = ssl_sect

[ssl_sect]
system_default = system_default_sect

[system_default_sect]
Groups = x25519:secp256r1:secp384r1
